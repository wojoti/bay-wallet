import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  buttonMargin: {
    marginVertical: 15,
  },
  breaklineSpacing: {
    marginBottom: 20,
    marginTop: 50,
  },
});
export default style;
