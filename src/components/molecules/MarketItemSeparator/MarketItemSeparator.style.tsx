import {StyleSheet} from 'react-native';
import {separator} from 'theme/colors';

const style = StyleSheet.create({
  wrapper: {
    height: 1,
    backgroundColor: separator,
    width: '85%',
  },
});
export default style;
