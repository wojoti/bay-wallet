import {StyleSheet} from 'react-native';
import {lightPrimary} from 'theme/colors';

const style = StyleSheet.create({
  btnStyle: {
    height: 60,
    backgroundColor: lightPrimary,
    borderRadius: 14,
  },
  sizer: {
    width: 155,
  },
});
export default style;
