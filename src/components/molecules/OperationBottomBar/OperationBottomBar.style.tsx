import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  wrapper: {
    position: 'absolute',
    width: '100%',
    height: 200,
    right: 0,
    bottom: 20,
    left: 0,
  },
});
export default style;
